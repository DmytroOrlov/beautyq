# Research / Reconciliation Record: BeautyQ Q2 Closeout State

**Feature**: `002-beautyq-q2-closeout` | **Date**: 2026-09-09 | **Plan**: [plan.md](plan.md)

This is the FR-001/FR-002 first-class reconciliation deliverable. Every dimension carries an explicit
classification and a durable evidence reference. It was produced by read-only repository/evidence
inspection only: no test run, no acceptance/bootstrap/verify execution, no source edit, no Git
index/ref/history mutation. All commands below are read-only (`git log/show/diff/ls-files/status`,
`find`, JSON reads) and re-derivable by an auditor.

## Derived evaluated state at reconciliation time

| Fact | Value |
| --- | --- |
| Branch | `develop` (tracking `origin/develop`) |
| `HEAD` | `47d90b9f2966f250e4c0a45c0267e8afde6b5d23` — "docs(beautyq): align current documentation with Spec Kit feature ownership" |
| Index | Clean — nothing staged |
| Worktree | Matches `HEAD` except untracked `specs/002-beautyq-q2-closeout/plan.md` + `research.md` (generated by this planning phase); `git diff --check` clean |
| Governance | Constitution v2.0.0 committed (`12538b26`), worktree byte-identical to `HEAD`; FR-017 stable — no older/staged constitution candidate |
| Human refs/stashes | Many human-owned local branches and 10 stash entries exist; none read as decision-relevant to the r8 chain; read-only, untouched (Principle I) |

## Reconciliation table (US-1 Acceptance Scenario 1 dimensions)

| Dimension | Owner | Derived state | Exact identity | Evidence reference | Classification | Consequence |
| --- | --- | --- | --- | --- | --- | --- |
| Committed history relevant to the accepted contract | Git history | Rotations 1–7 authorized/materialized; rotation-7 materialization `36d3f9dd` is the last application/protected-input change; only docs/Spec Kit/governance after it | `440fdf28..47d90b9f` | `git log --oneline`; `git diff --name-only 36d3f9dd..HEAD` | **derived** | Ladder state is read from these objects |
| Index/worktree vs `HEAD` | Git | Identical except this phase's untracked `specs/002` artifacts; nothing staged | — | `git status --porcelain`, `git diff --check` | **derived** | No hidden uncommitted evidence state |
| Plan's recorded pre-002 status claim | post-cutover plan | "prerequisites complete; Q2-B and Q2-C open" | — | `docs/gen2/BEAUTYQ_SEARCH_GEN2_POST_CUTOVER_PLAN.md` §"Q2 state" | **consistent, with refinement** (see Findings F-1) | Used as navigation only; evidence governs |
| Recovery authorization records | `BeautyQProtectedBreakGlassDisclosure.scala` | 12 committed authorizations; newest `q2-break-glass-exact-intent-recovery-rotation-8-v1` bound to app rev `36d3f9dd…`, corpus fp `ce020b49…`, policy fp `89189e94…`, check `metric-protected-slice:exact-intent-variants/success/10`, CompleteSlice(exact-intent, 8) | `789e5667` (committed at `HEAD`) | `git show 789e5667 -- …/BeautyQProtectedBreakGlassDisclosure.scala` | **exists, unconsumed** | Drives US-1 scenario 2 classification (below) |
| Recovery materialization records | reserve provenance + eval resources | Latest materialization = rotation 7 (`36d3f9dd`): slice replaced, fingerprints rebound, selection audit advanced to v8 (reserve v6: candidateCount 8, selected 8, **zero reserve cases left over**) | audit v8 source rev `66525825` | `…/protected/provenance/beautyq-protected-recovery-selection-audit-v8.json`; `git show --stat 36d3f9dd` | **rotation ≤7 only** | Rotation 8 requires fresh reserve authoring, not leftover selection |
| Protected-acceptance artifacts and bound identity | runner outputs (operator-evidence paths) | Most recent protected acceptance: **RED** — `passed=false`, failed check `metric-protected-slice:exact-intent-variants/success/10` observed `0.750000` vs `>=1.000000`; runner died `PROTECTED_ACCEPTANCE_RED`; invoked with `--application-revision 36d3f9dd5bc509948e288ace438e11860114419f`; artifacts carry the same corpus/policy fingerprints the r8 authorization binds | `36d3f9dd` | `target/search-gen2/protected/beautyq-protected-acceptance-gate.json` (+ aggregate/measurement + `q2-r7-protected-acceptance.private.log`, run 2026-08-21 08:33, 23 min after `36d3f9dd` committed 08:10); committed corroboration: r8 authorization binds these exact fingerprints | **RED at `36d3f9dd`; no green acceptance at any later revision** | Q2-B is open at a red boundary; ladder stops here until recovery completes |
| Candidate artifacts + candidate-review record | bootstrap output, coordinator records | Only candidate-era directory is `q2-candidate-20260806-084907-9573` (Aug 6): gate fingerprints `c9c0677f…` predate rotations 5–7 rebinds; its `bootstrap/` dir is empty; no `beautyq-accepted-baseline-candidate.json` from any post-r7 revision; no review-decision record anywhere | — | `.evidence-runs/q2-candidate/` inventory | **stale/absent** | Nothing to review; review is not the boundary |
| Promotion state | canonical tracked eval resource | `beautyq_accepted_evaluation_baseline_v1.json` **has never existed** in tracked history and is absent from the worktree; no promotion commit | — | `git log --follow` empty; `git ls-files beautyq-search-gen2-eval/src/main/resources/…` shows only the corpus resource | **open** | Downstream of acceptance; nothing to close |
| Verify state | verification manifest | No `beautyq-accepted-baseline-verification.json` outside unit-test scratch dirs under `target/codex-sbt/*-spec/` (test fixtures, not evidence) | — | filesystem inventory | **absent** | Verify waits behind the whole chain |
| Source/application revision identities | Git | App/protected sources unchanged from `36d3f9dd..HEAD` except `BeautyQProtectedBreakGlassDisclosure.scala` + its Spec (the r8 authorization record itself) — all changes since are docs/`.specify`/`.opencode`/`.review-bundles`/`README`/`AGENTS`/`.gitignore` | `36d3f9dd` → `47d90b9f` | `git diff --name-only 36d3f9dd..HEAD` | **derived** | FR-010 determination below |
| Other durable evidence | owner docs / checklists | 002 spec accepted at `HEAD`; plan/runbook/tech-spec ownership alignment committed (`47d90b9f`); feature 003/004 specs exist but are gated/non-normative | `edb9d001`, `47d90b9f` | `git log`, `specs/` tree | **consistent** | No competing status owner |

## FR-004 classification of the rotation-8 clue

Exactly one classification, each supported by the cited durable evidence — never by the clue itself:

**`RecoveryRotation8Authorization` = "authorization only"** (selection/materialization did NOT follow;
no evidence consumed it; it is not obsolete; it was not superseded).

- Authorization only: the record exists committed at `HEAD` (`789e5667`), bound to the RED identity
  `36d3f9dd` / `ce020b49…` / `89189e94…` observed 23 minutes earlier — the same fingerprints recorded in
  the RED gate artifact.
- Not "selection/materialization followed": no `Materialize … rotation 8` commit exists
  (`git log` tops out at rotation 7 = `36d3f9dd`); reserve audit v8 shows the v6 reserve was fully
  consumed by rotation 7.
- Not "evidence consumed it": `.evidence-runs/q2-break-glass/` has no rotation-8 disclosure directory
  (latest is `q2-r7-disclosure-20260820-124611-5671`); the disclosure runner records the consumed
  `authorizationId`, and no `…rotation-8-v1` record exists.
- Not "obsolete after later source changes": the runner consumes authorizations fail-closed against the
  **bound** corpus and policy content fingerprints (fingerprint mismatch → reject); protected inputs/policy
  and evaluation application sources are byte-unchanged since `36d3f9dd`. Precedent: the r7 disclosure
  executed after the r7 authorization commit while recording the bound pre-materialization revision
  `05392db3`
  (`.evidence-runs/q2-break-glass/q2-r7-disclosure-20260820-124611-5671/beautyq-protected-break-glass-disclosure.json`).
- Not "superseded by a later accepted boundary": no accepted post-r8 boundary exists; Q2-C artifacts
  (baseline resource, verify manifest) never appeared.

Therefore FR-004 is satisfied: acting on the rotation-8 recovery chain is legitimate **because
reconciliation independently concluded** the protected-acceptance boundary is red at exactly the content
fingerprints that the existing authorization binds to — not because a historical clue named rotation 8.

## FR-010 identity determination (post-materialization documentation changes)

The commits after `36d3f9dd` (`0de68ded`, `6d62171a`, `12538b26`, `edb9d001`, `47d90b9f`, plus `789e5667`)
touch only docs, Spec Kit assets, governance and the authorization record. Under the owners' evidence
semantics (acceptance/bootstrap/verify bind to the actual evaluated corpus/policy fingerprints; a Git
application revision is not part of the evaluation identity; r7 precedent shows an authorization-record
commit does not invalidate a bound-fingerprint consumption), **no pending green acceptance evidence exists
to be invalidated** — the next acceptance
evidence will be produced fresh against the post-materialization revision anyway. This is a determination,
not an assumption: the red evidence remains attributed to `36d3f9dd`, exactly where it was produced.

## Source-status reconciliation findings

- **F-1 (refinement, not conflict):** The plan's recorded pre-002 status "Q2-B open" is consistent with
  durable evidence, but understates its sub-state: Q2-B's latest protected acceptance is **RED against
  `36d3f9dd`** with a **live, unconsumed rotation-8 authorization bound to exactly that identity**. No
  plan text needs correction now (the plan already declares itself non-authoritative for current state);
  the refinement is carried here and at closeout under this feature's ownership (FR-013).
- **F-2:** Feature 001's research baseline commit `789e5667` is also the commit that recorded the r8
  authorization. Provenance noted; it does not change the authorization's durable status at `HEAD`
  (constitution v1.0.2 freeze applies to feature 001's own artifacts, not to this record's validity).
- **F-3:** The RED run's primary artifacts live under disposable `target/`; the committed r8
  authorization binds the same fingerprints/revision, so the red observation survives local `target`
  deletion. Attribution: red is evidence about `36d3f9dd` + `ce020b49…` + `89189e94…`, corroborated by
  both classes.

## Verdict

**First unfinished accepted boundary: Q2-B protected acceptance — currently RED at `36d3f9dd` — with the
owner-defined recovery continuation from the existing, live, unconsumed rotation-8 authorization.**

Per the owner ladder (prerequisite closure → protected acceptance → bootstrap → review → promotion →
verify → closeout) and the runbook's protected-workflow procedures, the remaining executable sequence
begins at: operator break-glass disclosure consuming
`q2-break-glass-exact-intent-recovery-rotation-8-v1` against revision `36d3f9dd`, then fresh
author/judge reserve authoring and rotation-8 materialization, then fresh protected acceptance under the
new revision.

Nothing upstream is blocked for evidence: all reconciliation dimensions are classified. No
`BLOCKED_NEED_EVIDENCE` is required at plan time. Execution-side gates (operator availability, external
ES/Qdrant/embedding resources, reserve-authoring capacity) are expected reportable boundary states, not
evidence gaps.

## Already-complete inputs (input only, never re-executed — FR-005)

- Q1/O0/O1 and recovery rotations ≤7 as committed history (plan §"Git and evidence boundaries"; git
  objects above) — including rotation-7 materialization `36d3f9dd` and the r7 authorization consumption
  (disclosure record at `.evidence-runs/q2-break-glass/q2-r7-disclosure-…`).
- Prerequisite closure of tracked protected inputs at the `36d3f9dd` rebind: the tracked audit
  `beautyq-protected-input-audit-v2.json` and 36d3f9dd's recorded deterministic freeze/hash reproduction
  stand as the durable prerequisite evidence; **no r8-era acceptance may silently reuse it after a
  rotation-8 rebind without the runbook-required fresh freeze/audit reproduction on the new bytes**.
- Constitution/governance alignment commits through `47d90b9f` — status: not part of the Q2 ladder,
  consumed as context.
