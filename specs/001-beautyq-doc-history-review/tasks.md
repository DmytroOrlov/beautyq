# Tasks: BeautyQ Search / Search Gen2 Documentation History Review

**Type**: FORENSIC DOCUMENTATION-HISTORY RESEARCH task list — not a software implementation plan.

**Inputs**: `.specify/memory/constitution.md` **v1.0.2** (non-negotiable, supremacy clause) · [`spec.md`](./spec.md) · [`plan.md`](./plan.md) · [`checklists/research-quality.md`](./checklists/research-quality.md) (**45/45 reviewed — contract approved for decomposition**)

**Baseline**: `789e56674145a45c31e2e25d997c60b359431a48` on branch `develop` (constitution §1; no feature branch is created or switched — constitution §4 / FR-004).

**Ordering rule**: execution order is the evidence dependency chain of Plan §21, **not** spec user-story order. `[RS1]`–`[RS4]` are Research Story traceability labels only; they never define sequence. RS3 (adversarial challenge) is mandatory for this run with no skip path (FR-040).

## Global execution rules (binding on every task)

* **Write scope**: only `.review-bundles/docs-history-speckit/{COVERAGE.tsv,WORKING.md,BLIND.md,REPORT.md}` and `specs/**` harness bookkeeping. Canonical BeautyQ content (`README.md`, `AGENTS.md`, `docs/**`, source, tests, build, CI) is immutable (constitution §5 / FR-005).
* **Exactly four analytical outputs** (FR-041 / constitution §23). Never create `candidate.tsv`, `refs.txt`, `evidence.csv`, `decisions.md`, `threads.md`, `findings.md`, `transfer.md`, `absence.md`, `scratch.md`, `report-draft.md`, raw Git dumps, diff archives, or command transcripts. Candidate accounting, exclusion ledger, ref inventory, absence records, and challenge state live inside `WORKING.md`.
* **Read-only Git only** (constitution §4): `log`, `show`, `diff`, `grep`, `rev-list`, `ls-tree`, `cat-file`, `blame`, `show-ref`, `for-each-ref`, `branch --contains`. No fetch/pull, checkout/switch, branch/tag/stash mutation, reset/rebase/merge/commit/clean, config or ignore-rule edits.
* **DOC-ONLY evidence** (constitution §3 / FR-006–011): documentary content + commit metadata only; mixed code+docs commits via their documentation portion only; never open source/tests/build/CI to validate a documentary claim.
* **Failure semantics (FR-053)**: any hard-gate failure emits `Status: FAIL` with all twelve FR-053 required fields and stops all downstream tasks. There is no "continue anyway" path. BLIND freeze emits `Status: PAUSED — awaiting challenge pack`, never FAIL and never COMPLETE.
* **Checklist handling**: `checklists/research-quality.md` is already reviewed 45/45 — the executor MUST NOT change any of its markers and MUST NOT re-evaluate it; `checklists/requirements.md` is not a historical evidence source; checklist marker state is never historical evidence.
* **No findings in this file**: `tasks.md` is method-only. BeautyQ-specific conclusions, epochs, thread names, and hypotheses MUST NOT be prepopulated here or anywhere before blind derivation (constitution §9).
* **Materiality** = FR-052 (single authoritative definition). **Provenance unit** = constitution §11 v1.0.2 / FR-026 (per material evidence anchor and per atomic material analytical statement; composite findings may combine `CD`/`RD`/`ND`/`CM`/`HI`/`RJ`).

---

## Phase 1 — Research preflight (BLOCKS all research)

**Purpose**: verify the frozen baseline, prove previous-review isolation, then freeze the local ref universe and enumerate documentary paths (Plan §6, Plan §21 Phase 1).

**Invariant**: `Run-A isolation PASS → historical documentary discovery`. Local REF NAMES/REF IDENTITIES may be enumerated as mechanical repository-state information only after T001 and T002 have both passed, according to T003's gate; ref-name/ref-identity enumeration is not itself historical documentary evidence, and NO documentary-history traversal for candidate/path discovery occurs before the T002 isolation gate passes.

**Preflight write rule**: T001–T004 retain their values transiently in-session. No run log, no FAIL log artifact, and no other file is created before T005; the four analytical outputs remain exactly four. On any pre-T005 failure, emit FR-053 `Status: FAIL` in the assistant response and stop — nothing is written. T005 is the FIRST analytical write and atomically persists the successful preflight data into `WORKING.md` §A, where successful isolation appears solely as `Previous-review isolation precondition: PASS`.

- [x] T001 [RS1] Record the execution-start baseline **in-session only** (no file is written by this task): repository root, `git rev-parse HEAD` (MUST equal `789e56674145a45c31e2e25d997c60b359431a48`), current branch (MUST equal `develop`), and the research-start timestamp. FAIL CLOSED via FR-053 `Status: FAIL` emitted in the assistant response on HEAD mismatch or wrong branch (constitution §1, FR-001–003); no candidate discovery, no artifact creation, and no fetch/pull/branch change is permitted as a repair.
- [x] T002 [RS1] Verify the previous-review (Run-A) isolation precondition: confirm that earlier BeautyQ documentation-history Run-A outputs are physically absent from the repository working tree AND from every other location this run will search, WITHOUT reading, locating, moving, renaming, or deleting Run-A material and WITHOUT satisfying the condition via ignore rules or search exclusion (constitution §2, FR-012–015, Plan §6). On success hold the result in-session as `Previous-review isolation precondition: PASS` (persisted by T005). If PASS cannot be established safely, STOP with FR-053 `Status: FAIL` emitted in the assistant response — no FAIL artifact is created — (observed condition must reveal no Run-A content or conclusions) and require the user to isolate. Never create a task step that inspects Run-A contents. This gate MUST complete before T004 and before any Phase 2 discovery. Gate: T001.
- [x] T003 [RS1] Freeze the locally present Git-ref universe read-only with `git show-ref` / `git for-each-ref` / `git rev-list` and hold the enumeration in-session for the `WORKING.md` §A compact inventory (FR-018, FR-051, SC-013). Do NOT create a ref-dump file; do NOT expand the universe later in this run. Enumerating ref names/identities is mechanical repository-state inventory, NOT documentary-history candidate/path discovery. This freeze bounds the review and MUST complete before the first historical documentary-path traversal at T004. Gate: T001, T002.
- [x] T004 [RS1] Perform the initial documentary-**path** enumeration only (read-only `git log --name-status`/`--diff-filter=ADR` style navigation over committed `README.md`, committed `AGENTS.md`, and `docs/**` plus deleted/renamed/historically visible documentary paths). No content analysis, no relevance judgement, no per-commit disposition. This is the first historical-documentary traversal of the run and occurs ONLY after T001 AND T002 have both passed AND the T003 local-ref universe is frozen; it is NOT parallel-safe with the isolation gate. Reconciled with T003 before any analysis at T006. Gate: T001, T002, T003.
- [x] T005 [RS1] Create `.review-bundles/docs-history-speckit/WORKING.md` **only after T001–T004 all pass**, with the full 15-section skeleton: A. Run Metadata & Ref Inventory · B. Candidate Corpus Accounting (incl. Candidate Corpus Exclusion Ledger) · C. Coverage Summary · D. Document Lineage Ledger · E. Evidence Ledger · F. Decision Ledger · G. Decision Threads · H. Retrospective Claim Audit · I. Transfer Audits · J. Absence Claims · K. Blind Open-Coding Notes · L. Challenge Phase · M. Counter-Evidence · N. Current-State Reread · O. Final Mechanical Checks. This is the first analytical write of the run: it atomically persists the in-session preflight values — §A with T001 baseline values, the T003 compact ref inventory, and the single line `Previous-review isolation precondition: PASS` (no other isolation content, no FAIL form of this line) (FR-042, constitution §24). Gate: T001, T002, T003, T004.

**Checkpoint**: preflight complete and persisted into §A — analytical research may begin; no analytical file existed before T005.

---

## Phase 2 — Candidate documentary universe (the denominator)

**Purpose**: build the reproducible discovery set that later proves exhaustive accounting (FR-046–048, Plan §7).

- [x] T006 [RS1] Reconcile T003 + T004 and assemble the reproducible **candidate documentary commit universe** reachable from the locally present refs frozen at T003: committed `README.md` history, committed relevant `AGENTS.md` history, relevant `docs/**`, historical/deleted/renamed documentary paths, copied/replaced/superseded paths, paths visible only historically, and documentary content reachable from locally present non-primary refs. Discovery MUST NOT be restricted by churn threshold, commit-message keyword, current-paths-only, Gen1/Gen2 date assumptions, known epochs, expected findings, or prior-review conclusions (FR-016/017/046/048, SC-001). **Persist the exact universe in `WORKING.md` §B as a compact in-file ledger** — one entry per UNIQUE COMMIT (never per `(ref, commit)` pair: one SHA reachable from multiple local refs or discovered via multiple paths is ONE candidate, and its entry consolidates all discovery provenance instead of double-counting it) recording at minimum: commit SHA; commit date; the documentary path(s) that caused its discovery; and the discovery source ref / canonicalized ref-origin note sufficient to understand why it entered the universe. The ledger makes NO relevance judgement at discovery time — it is a discovery set, not evidence (Plan §7) — and creates no new file. The candidate count is then established mechanically FROM this exact persisted set (not from an unpersisted transient list); §B membership is the authoritative denominator that T010 must resolve member-by-member and that T014 reconciles by exact SHA. Gate: T005.
- [x] T007 [RS1] Assess every locally present non-primary ref (branches, tags, reference/stash snapshots) for unique documentary substance and write the compact inventory + per-ref assessment into `WORKING.md` §A. The rule is unambiguous: non-primary-ref assessment MUST NOT DOUBLE-COUNT commits; it MAY add a previously absent unique commit exactly once. Commits already represented in the T006 Candidate Universe are NEVER counted again merely because another ref reaches them; shared commits may instead have their consolidated discovery/ref provenance enriched. If the non-primary-ref assessment surfaces a genuinely new unique documentary commit that is not already represented in §B, append exactly one new unique-SHA candidate entry to `WORKING.md` §B, record its discovery/ref provenance, and consolidate it normally if it is later found through another path or ref. After T007 completes, the resulting §B unique-SHA set is the current complete candidate denominator for initial batching (T009). Record that no remote-only history was audited (FR-018, FR-051, SC-013, spec edge case 21). Gate: T006.

**Checkpoint**: initial denominator complete — after T006 + T007 the §B unique-SHA set is the authoritative candidate denominator from which T009 derives runtime batches; `COVERAGE.tsv` alone is never treated as the completeness proof.

### Late Candidate Discovery Rule (authoritative — visible to Phases 2–6; applies ONLY BEFORE BLIND FREEZE)

If, during T010–T025, read-only documentary inspection legitimately surfaces a documentation-changing commit that satisfies the FR-046 candidate-discovery boundary but is absent from the current §B Candidate Universe:

1. Do NOT ignore it because initial discovery has completed; do NOT classify it outside the denominator; do NOT create a new artifact.
2. Append it to `WORKING.md` §B as exactly one unique-SHA Candidate Documentary Commit with: SHA; date; documentary path(s) causing discovery; discovery provenance/ref/path-family note; and a `late-discovered` bookkeeping note stating which task/inspection surfaced it.
3. If the SHA already exists in §B: do not add another candidate; merge the new discovery provenance into the existing candidate entry.
4. Mechanically update the candidate total.
5. Re-derive the affected REMAINING operational batch boundaries in §B — do not rewrite already completed batch history and do not reinterpret batch boundaries as epochs.
6. Process every newly added unresolved candidate under the full T010 documentary-content / classification / evidence / compression discipline.
7. Re-run the applicable Phase-3 post-loop audits: T011 classification-basis audit; T012 evidence-ledger audit; T013 final compression/integrity audit; T014 exact-set and arithmetic reconciliation.
8. If discovery occurred after Phase 4 or Phase 5 analysis had already used the prior corpus: re-evaluate and update any affected lineage, decision, thread, causality, retrospective, transfer-seeding, open-coding, positive-finding, or epoch analysis before T025 is allowed to pass.
9. T025 MUST NOT pass until: no known candidate remains outside §B; the updated T014 exact-membership reconciliation passes; and downstream blind analysis reflects the updated corpus.

This is a controlled re-entry into the existing analytical loop, NOT creation of new static tasks; task IDs remain unchanged. The rule does NOT apply after BLIND FREEZE: BLIND remains sealed under the existing constitution/spec rules; post-freeze candidate discovery is governed by the separate **Post-BLIND Candidate Discovery Rule** (stated immediately before Phase 7), and any post-freeze factual problem remains governed by the `FACTUAL CORRECTION` convention and the governing failure/uncertainty rules.

---

## Phase 3 — Candidate reconciliation and chronological documentary inspection

**Purpose**: resolve every candidate to exactly one accounting outcome by reading documentary content, chronologically, in runtime-derived operational batches (FR-019–025, FR-047/048, Plan §8/§10).

- [x] T008 [RS1] Create `.review-bundles/docs-history-speckit/COVERAGE.tsv` (only now, after preflight passed and research execution began) with the exact tab-separated header and column order `commit`·`date`·`paths`·`disposition`·`reason`·`decision_threads`·`evidence_ids`; allowed dispositions ONLY `deep`, `semantic-scan`, `clerical`, `duplicate` — no fifth disposition, no `metadata-only`/`sampled`/`probably irrelevant` (FR-019, FR-020, constitution §6). Gate: T005.
- [x] T009 [RS1] Derive **runtime** chronological operational batch boundaries from the actual Phase-2 candidate count/set AFTER T007's non-primary-ref assessment has completed — runtime batch derivation MUST NOT occur before T007 — using the current complete §B candidate set and documentary density (default ≈25–50 candidate commits per batch, adapted where needed), and record the chosen boundaries compactly in `WORKING.md` §B together with the explicit statement `Operational batches are NOT historical epochs.` Do NOT invent commit ranges or dates now; do NOT encode fake batch boundaries into this file (Plan §10, constitution §15, FR-032). Gate: T006, T007.
- [x] T010 [RS1] **Self-contained iterative/reusable runtime loop over ALL batches — never re-open `tasks.md` for new batches; never delegate a substep to a downstream task.** Repeat the complete per-batch subcycle below for each T009 batch in chronological order until EVERY SHA in the persisted T006 Candidate Universe has been processed exactly once. Per batch, in order: (1) inspect every candidate in the batch sufficiently by documentary content; (2) resolve each as relevant vs excluded-after-inspection; (3) if excluded → add one Candidate Corpus Exclusion Ledger record in `WORKING.md` §B (commit; date; documentary path(s); exclusion reason; sufficient-inspection level) and NO COVERAGE row; (4) if relevant → add exactly ONE `COVERAGE.tsv` row; (5) assign one of `deep`/`semantic-scan`/`clerical`/`duplicate`; (6) promote to `deep` whenever constitution §7 / FR-021 triggers apply (including modal transitions of FR-038); (7) apply the classification-basis rules of T011 inline (never classify or exclude from commit message, filename, churn/numstat, date, or milestone label alone); (8) CREATE/UPDATE material evidence anchors in `WORKING.md` §E during inspection using stable `E-xxxx` IDs, one provenance class per anchor from constitution §11 v1.0.2 / FR-026, materiality by FR-052 ONLY; (9) update lineage observations in §D; (10) update decision observations in §F where material; (11) run the `WORKING.md` compression checkpoint inline (preserve stable IDs, evidence anchors, lineage observations, decisions, contradictions, and unresolved uncertainty; drop raw Git commands, raw dumps, long copied diffs, diary narration, repetitive discovery notes, and obsolete guesses); (12) verify every candidate in that batch has exactly one outcome; (13) only then proceed to the next batch. If inspection at any point discovers a documentation-changing FR-046 candidate absent from §B, apply the Late Candidate Discovery Rule (Phase 2) before continuing to final reconciliation. `decision_threads` and evidence-ledger finalization may remain open during early batches; `evidence_ids` on COVERAGE rows are filled as anchors are created here and are audited post-loop by T012 (FR-021–024, FR-026/052, FR-042, FR-047, Plan §8/§10). Gate: T008, T009.
- [x] T011 [RS1] **POST-LOOP audit** of the classification basis of every candidate outcome produced by T010 (does not execute between batches): verify across all batch results that NO candidate was classified or excluded from commit message alone, filename alone, churn/numstat alone, date alone, or milestone label alone; that low churn never suppressed semantic importance (a one-line modal/ownership change can be `deep`); that genuinely uncertain relevance was included in `COVERAGE.tsv` with a content-based disposition; that `clerical` applies ONLY to already-relevant, mechanically editorial changes and NEVER means "unrelated"; and that every `duplicate` has proven documentary equivalence against the other lineage with the equivalent commit recorded, never branch-name inference. Any violation is corrected in the Phase-3 ledgers before T014 (FR-022–024, FR-048, constitution §7/§16, SC-002, spec edge cases 1, 2, 7, 18, 20). Gate: T010 exhausted.
- [x] T012 [RS2] **POST-LOOP audit** of the `WORKING.md` §E Evidence Ledger ACCUMULATED DURING T010 (this task does not create the first evidence anchors; it verifies and completes the ledger T010 maintained): every material anchor has a sequential/stable `E-0001`, `E-0002`, … ID; exactly one provenance class from constitution §11 v1.0.2 / FR-026 (`CD`/`RD`/`ND`/`CM`/`HI`/`RJ`); commit; date; documentary path; heading/section where available; concise paraphrase (not copied quotations); and thread association once known (per-anchor provenance). Materiality uses FR-052 ONLY. Verify that all material evidence gathered during Phase 3 is represented, that every COVERAGE `evidence_ids` reference resolves to an actual Evidence Ledger anchor, and that atomic material analytical statements created during Phase 3 carry an explicit evidentiary posture per constitution §11 v1.0.2 / FR-026 where applicable. Verify the hard weighting constraints on ledger content: `RD`≠`CD`, `CM`≠`CD`, `HI` remains inference and not documentary fact, and `RJ` remains reviewer judgment and not historical fact. This audit verifies ONLY evidence/provenance structures that can exist after T010; final/composite findings do not exist yet in Phase 3, so finding-level provenance is enforced downstream — T034 owns proposed High/Critical counter-evidence and severity/confidence reassessment, and T037 owns final finding composition, per-anchor provenance, and the High/Critical evidence rule. Gate: T010 exhausted.
- [x] T013 [RS1] **Phase-3 final compression and compression-discipline audit** (not a substep T010 must invoke between its own iterations — T010 performs per-batch compression inline): verify that the T010 loop actually compressed `WORKING.md` after each substantial batch under the "preserve evidence and conclusions, not the path by which they were discovered" discipline (constitution §24, FR-042, Plan §10), then perform ONE final Phase-3 compaction before reconciliation, preserving stable IDs, evidence anchors, lineage observations, decisions, contradictions, and unresolved uncertainty and dropping raw Git commands, raw dumps, long copied diffs, diary narration, repetitive discovery notes, and obsolete guesses. Gate: T010 exhausted, T011, T012.
- [x] T014 [RS1] Phase-3 lifecycle and accounting reconciliation **before any blind synthesis** — SET-IDENTITY reconciliation, not count comparison: (a) the **current authoritative §B** `CandidateUniverseSHASet` — the persisted T006 universe after the T007 assessment and after all known Late Candidate Discovery Rule additions — equals `CoverageRelevantSHASet ∪ ExclusionSHASet`, with `CoverageRelevantSHASet ∩ ExclusionSHASet = ∅` and every candidate occurring exactly once in the union (equal counts never substitute for exact SHA membership, which would otherwise hide one omitted SHA plus one duplicated SHA); (b) every relevant row has a final allowed disposition and every relevant/material row links evidence IDs available from Phase-3 evidence collection; (c) verify BOTH arithmetic identities mechanically from source — `candidate_documentary_commits = relevant_documentary_commits + excluded_after_inspection` and `relevant_documentary_commits = deep + semantic-scan + clerical + duplicate` — and write both into `WORKING.md` §C. **`decision_threads` MAY remain blank/unresolved at T014 because thread synthesis has not occurred yet (T018); this is expected, is NOT a failure, and MUST NOT be satisfied with placeholder thread IDs** — final thread-ID backfill belongs to T025. Unreconcilable accounting is an FR-053 `FAIL`, not an approximation (FR-025, SC-001, SC-003, constitution §8). Gate: T013.

**Checkpoint**: corpus accounted for; lineages/decisions accumulated; no interpretation is permitted to outrun the ledger.

---

## Phase 4 — Lineage and decision reconstruction (no findings yet)

- [x] T015 [RS2] Reconstruct documentary lineage in `WORKING.md` §D from the inspected history: creation · rename · copy · replacement · supersession · consolidation · deletion · retirement, with original role, historical owners, successor, and current status per important lineage. Lineage MUST rest on documentary evidence; filename similarity alone is NOT lineage proof; renames are not evidence of loss or of a new decision (Plan §11, FR-037, constitution §7, spec edge cases 4–6). If T015 surfaces a previously absent FR-046 candidate commit (e.g. a historical predecessor/successor that candidate discovery missed), invoke the Late Candidate Discovery Rule and re-run the affected Phase-3 reconciliation and affected Phase-4/5 analysis before continuing — T015 MUST NOT silently continue with an incomplete denominator. Gate: T014.
- [x] T016 [RS2] Build the Decision Ledger in `WORKING.md` §F with stable IDs `D-001`, `D-002`, … ; each material decision records supporting evidence anchors, date, documentary owner, problem addressed, decision/rule, rationale, normative strength, temporary/permanent framing, and a "later known change" field left blank until actually encountered. Do NOT write final findings in this phase (Plan §12, FR-052). Gate: T015.
- [x] T017 [RS2] Track requirement-strength drift across the ledger: `may→should→must`, `optional→default→mandatory`, `experimental→supported→required`, `temporary→current/permanent`, `goal→deferred→non-goal`, `proposal→normative`, `historical→current`, `candidate→required`, `allowed→forbidden`; record each transition as a decision/transition entry with its evidence anchors, never using line churn as an importance proxy (constitution §16, FR-038). Gate: T016.

---

## Phase 5 — Blind reconstruction: threads, audits, open coding, epochs

**Purpose**: independent evidence-only synthesis before any hypothesis pack exists (constitution §9/§14/§15, FR-030–032).

- [x] T018 [RS2] Derive Decision Threads in `WORKING.md` §G with stable IDs `T-01`, `T-02`, … **only after** chronological inspection (T010) and the decision ledger (T016) exist. Thread names MUST be neutral and evidence-derived; do NOT prepopulate BeautyQ-specific thread names. Each thread records: initial problem; first documentary decision; material changes; rationale evolution; ownership evolution; requirement-strength evolution; temporary/permanent framing; current documentary state; unresolved contradictions. HARD GATE: same component ≠ same decision thread — do not merge decisions merely because they concern one system; existence / role / result ownership / lifecycle / evaluation / product value are methodology examples only, never a predetermined thread set (constitution §14, FR-030, Plan §12, spec edge case 19). Gate: T017.
- [x] T019 [RS2] Cross-thread causality audit (pre-BLIND hard gate): inspect every causal sentence in §G/§F; same-thread inference may be recorded as `HI`; cross-thread causality is permitted ONLY where documentary evidence explicitly links the threads, otherwise record sequence/correlation as `HI` and do not state causation. Temporal order alone never becomes causality (FR-031, constitution §14). Gate: T018.
- [x] T020 [RS2] Retrospective-claim audit into `WORKING.md` §H: identify significant later/current statements about earlier intent ("originally", "the purpose was", "historically", "this was temporary", "this was never intended", "Gen2 was designed to", "the framework exists because", "the original goal was"); classify the later statement `RD`, locate contemporaneous evidence, compare, and classify each as accurately preserved / simplified but fair / stronger than contemporaneous evidence / changed interpretation / possible historical retcon / unverifiable. Current documentation MUST NOT override contemporaneous evidence (constitution §12, FR-027, spec edge case 9). Gate: T018.
- [x] T021 [RS2] Pre-BLIND transfer seeding into `WORKING.md` §I: identify major documentary transfer candidates — plan retirement, roadmap retirement, handoff retirement, ADR/spec replacement, source-truth relocation, generated-vs-handwritten authority change, migration-document retirement, audit/proof-document retirement — and seed enough records to support independent blind interpretation. Final transfer JUDGMENTS are deferred until after the challenge phase (T032) (Plan §16, FR-037). Gate: T018.
- [x] T022 [RS2] Blind open-coding pass into `WORKING.md` §K: code from evidence without a hypothesis pack across the categories candidate architectural values; recurring problems; ownership patterns; documentary-governance patterns; goal changes; temporary/permanent tension; proof/evaluation patterns; migration residue; simplification patterns; successful corrections. These are CODING CATEGORIES, not expected conclusions; the run remains free to conclude no meaningful drift, justified complexity, successful ownership design, or no significant source-truth problem (constitution §9, Plan §13d, FR-033/034 keep drift and complexity categories distinct). Gate: T019, T020.
- [x] T023 [RS2] Derive candidate POSITIVE findings before BLIND from historically supported evidence (durable invariant preservation, clean or mostly clean ownership consolidation, rationale preservation, deliberate retirement of temporary plans, artifact clarification, explicit scope containment, honest single-domain limits, useful simplification, appropriate safety constraints, successful correction of earlier documentary mistakes) under the same evidence discipline as negative findings; this run is not criticism-mining (constitution §22, FR-035). Gate: T022.
- [x] T024 [RS2] Derive candidate historical periods/epochs **only after** thread reconstruction, and only from actual material change in dominant problem, architectural thesis, values, ownership, source truth, proof burden, migration state, documentation organization, or product goal. NEVER derive epochs from operational batch boundaries, milestone labels, file counts, commit counts, or arbitrary calendar segments; record boundaries as `HI`/`RJ` unless contemporaneously named (constitution §15, FR-032). T024 is the final interpretive synthesis step of Phase 5 before the Phase-6 pre-freeze gate. Gate: T019, T020, T021, T022, T023.

---

## Phase 6 — BLIND validation and freeze (HARD TASK-GRAPH BOUNDARY)

- [x] T025 [RS2] Pre-freeze gate sweep (all must pass before BLIND is written) and **owner of `decision_threads` finalization** (thread IDs do not exist before T018 derives them and must never be invented earlier): (1) candidate accounting reconciled per T014; (2) `COVERAGE.tsv` lifecycle reconciled per T014; (3) material evidence IDs finalized with provenance per T012; (4) **backfill/finalize the applicable `COVERAGE.tsv.decision_threads` values with the final `T-xx` IDs from the independently derived §G thread model of T018** — rows for which no material thread applies remain blank ONLY when justified under FR-052, with no placeholder values; (5) verify that no BeautyQ-specific thread ID existed anywhere before the T018 blind derivation; (6) retrospective audit state complete per T020; (7) cross-thread causality discipline verified per T019; (8) verify that no user-provided BeautyQ historical hypothesis pack was seen, requested, reconstructed, inferred from conversation/history, or searched for during this phase (FR-039/040, FR-052, constitution §9); (9) no known late candidate remains unresolved — the authoritative §B universe and the latest T014 reconciliation reflect all documentary candidates discovered before freeze (Late Candidate Discovery Rule). Any failure → FR-053 `Status: FAIL`, BLIND not created. Gate: T018, T021, T023, T024.
- [x] T026 [RS2] Create `.review-bundles/docs-history-speckit/BLIND.md` containing exactly the required sections A–J (no eleventh analytical section): A. Freeze Metadata (frozen HEAD, branch, repository root, research-start ts, freeze ts, local ref-universe summary, isolation-precondition result, **research history range discovered** — earliest and latest relevant documentary date/commit at freeze, derived only from locally present refs, never inferred remote history) · B. Coverage at Freeze (exact candidate/excluded/relevant + deep/semantic-scan/clerical/duplicate, showing BOTH FR-025 identities) · C. Documentary Lineages · D. Decision Threads · E. Candidate Historical Periods · F. Candidate Architecture Values (durable + changing) · G. Candidate Findings (no hypothesis influence) · H. Candidate Positive Findings — across C–H every material evidence anchor retains its E-ID and provenance class, and every atomic material analytical statement makes its evidentiary posture explicit per constitution §11 v1.0.2 / FR-026, so BLIND itself carries evidence provenance rather than merely referring to `WORKING.md` §E (a compact provenance legend may appear but is not sufficient on its own) · I. Unresolved Questions and Uncertainty (ambiguity not hidden) · J. Independence Declaration stating the substance: `No user-provided BeautyQ historical hypothesis pack was seen, requested, reconstructed, or used during this blind phase.` BLIND remains a compressed independent synthesis, not a wholesale duplication of `WORKING.md` (FR-039, constitution §10/§11, Plan §14). T027 remains the independent post-creation FR-039 validation. Gate: T025.
- [x] T027 [RS2] Validate `BLIND.md` against sections A–J and FR-039's required contents, seal it (thereafter immutable except clearly appended `FACTUAL CORRECTION` notes — constitution §10), and record in `WORKING.md` §L the exact state `Challenge phase state: awaiting pack`. Gate: T026.
- [x] T028 **STOP EXECUTION AND RETURN TO USER — EXTERNAL DEPENDENCY GATE, NOT A COMPLETION FOLLOWED BY T029.** Do NOT begin Phase 7. Do NOT request, preview, anticipate, reconstruct, infer, or search for the challenge pack. Do NOT create or finalize `REPORT.md`. Emit the FR-053 pause report with ALL required fields: `Status: PAUSED — awaiting challenge pack`; frozen HEAD; completed phase/task ID (`Phase 6 / T028`); exact paths of existing analytical outputs; BLIND path and sealed status; `challenge state: awaiting pack`; exact candidate / excluded / relevant / deep / semantic-scan / clerical / duplicate counts at freeze; both reconciled FR-025 identities; the canonical diff state; and the explicit statement `REPORT.md has not been finalized and completion is NOT claimed.` (FR-040, constitution §10/§27, Plan §14, SC-007). Gate: T027.

**Checkpoint**: BLIND FREEZE. Autonomous execution ends here by contract.

---

### Post-BLIND Candidate Discovery Rule (authoritative — applies ONLY after T027 has sealed `BLIND.md` and ONLY before T045 COMPLETE)

**Scope and external boundary.** This rule applies only after T027 has sealed BLIND, only before T045 COMPLETE, and only to documentary commits inside the already frozen locally-present-ref universe (T003); NEVER to remote or fetched history — no fetch/pull may be used to reach a candidate. This rule does **not** authorize autonomous work after T028: T028 still requires `STOP EXECUTION AND RETURN TO USER`, no post-BLIND task runs until the user actually supplies the challenge pack, and the rule becomes usable only after T029 is legitimately unlocked by that external user event.

If permitted post-BLIND documentary research in Phases 7–10 surfaces a documentation-changing commit satisfying FR-046 that is absent from the authoritative `WORKING.md` §B Candidate Universe:

1. Do NOT ignore it; do NOT create a fifth analytical artifact; do NOT rewrite the original BLIND narrative; do NOT alter the frozen-ref universe.
2. Append it to `WORKING.md` §B as exactly one unique-SHA candidate recording: SHA; date; documentary path(s); discovery provenance / ref/path-family note; the `post-BLIND-discovered` marker; and the task/analysis that surfaced it.
3. If the SHA already exists in §B: do not create a second candidate; merge the new provenance into the existing entry.
4. Inspect it under the same DOC-only content/classification discipline as T010: relevant vs excluded-after-inspection; if relevant, exactly one `COVERAGE.tsv` row with one of `deep` / `semantic-scan` / `clerical` / `duplicate`; exclusion remains outside COVERAGE and is not a fifth disposition; evidence anchors and provenance per constitution §11 v1.0.2 / FR-026; FR-052 materiality; lineage/decision observations as needed.
5. Mechanically recompute: authoritative candidate total; relevant/excluded totals; disposition totals; exact §B SHA-set identity; both FR-025 arithmetic identities.
6. Update `WORKING.md` with the new final corpus accounting and record a compact `freeze → final corpus delta` explaining the changed SHA membership/counts.
7. Re-run every downstream analysis materially affected by the new candidate: lineage; decisions; thread interpretation; causality; retrospective audit; transfer audit; absence claims; challenge evaluation where applicable; proposed findings / counter-evidence; current-state reread; REPORT content.
8. If it creates or changes a material proposed final finding, apply the Late Proposed Finding Rule; do not bypass §M.
9. If it materially affects a supplied challenge hypothesis, perform a supplemental challenge evaluation against the already-supplied challenge pack, record the delta in `WORKING.md` §L, preserve the fact that the mandatory challenge occurred, and do NOT rewrite the original BLIND analysis.

**BLIND preservation under post-BLIND candidate discovery.** BLIND remains the immutable record of what was known at freeze: NEVER rewrite its original A–J content; NEVER silently replace its freeze counts with final counts; §B remains explicitly the `Coverage at Freeze` snapshot. If the newly discovered candidate changes a MATERIAL factual premise represented in BLIND, use only the constitution-permitted append-only mechanism: a clearly labelled `FACTUAL CORRECTION` note recording what is necessary for auditability — that discovery occurred post-freeze; the candidate SHA/date/path; which frozen BLIND factual element is affected; the corrected factual state; and a pointer to the updated WORKING evidence/ledger entries. The original BLIND wording remains visible and unchanged; the correction append must not be made to look like part of the original independent blind narrative, and a post-freeze correction does not retroactively become blind evidence.

**Freeze counts vs final counts.** BLIND contains exact **freeze-time** counts and identities. Final COVERAGE / final WORKING / REPORT / the completion response contain exact **final** counts after all allowed pre-final additions/reclassifications. If those differ because of post-BLIND candidate discovery, `WORKING.md` records the compact `freeze → final corpus delta`; final counts are NOT required to equal the historical BLIND freeze snapshot. The final cross-surface equality gate applies to final COVERAGE, final WORKING, final REPORT, and the completion response; BLIND's freeze snapshot is audited separately for internal correctness at freeze.

**Controlled append/reconcile vs FR-053 FAIL.** Post-BLIND candidate discovery is not an automatic failure: `incorporable within the frozen DOC-only universe → append/reconcile` under this rule. FR-053 `Status: FAIL` applies only when this rule cannot be completed safely — e.g., candidate accounting cannot reconcile; the required evidence would need forbidden source/test/build/CI inspection; the commit lies outside the frozen local-ref universe and would require fetch/pull; Run-A contamination occurs; a required material audit cannot be brought to a valid state before completion; or canonical immutability fails. This is a conditional execution correction, not a new static task; task IDs remain unchanged and the four-analytical-output limit is intact.

---

## Phase 7 — Mandatory adversarial challenge — **BLOCKED UNTIL USER SUPPLIES POST-BLIND CHALLENGE PACK**

**No task in this phase may start without the external user-supplied pack. There is no skip path (FR-040). The pack's contents are deliberately not predicted here.**

- [x] T029 **BLOCKED UNTIL USER SUPPLIES POST-BLIND CHALLENGE PACK** [RS3] Record `WORKING.md` §L state `received`; keep `BLIND.md` sealed and unmodified (appends only for genuinely required `FACTUAL CORRECTION` notes); do not rewrite the independent first-pass narrative (FR-040, constitution §10, Plan §15). Gate: external pack supplied.
- [x] T030 **BLOCKED UNTIL USER SUPPLIES POST-BLIND CHALLENGE PACK** [RS3] Evaluate every supplied hypothesis against the existing documentary corpus using the user-supplied hypothesis classification scheme; for each, record supporting evidence AND the results of an active disconfirming-evidence search in `WORKING.md` §L, with provenance per anchor (FR-026/029, constitution §11/§18). The pack must not overwrite independently derived threads. If a previously absent FR-046 candidate is discovered post-BLIND during challenge evaluation, apply the Post-BLIND Candidate Discovery Rule before Phase 7 can be considered complete. Gate: T029.
- [x] T031 **BLOCKED UNTIL USER SUPPLIES POST-BLIND CHALLENGE PACK** [RS3] Set `WORKING.md` §L state `challenge performed`. RS3 is mandatory: while the state is `awaiting pack`, Phases 8–11 and `REPORT.md` MUST NOT begin and the run is reported as paused, never complete (FR-040, spec edge case 22, SC-007). Gate: T030.

---

## Phase 8 — Transfer, absence, and counter-evidence audits — **BLOCKED UNTIL `challenge performed` (T031)**

- [x] T032 [RS4] **BLOCKED UNTIL USER SUPPLIES POST-BLIND CHALLENGE PACK (state `challenge performed`)** Complete every major transfer audit in `WORKING.md` §I along the FR-037 chain: old owner → transition → claimed destination → decision preserved? → rationale preserved? → alternatives preserved/lost? → history/status intentionally discarded? → orphaned information? → reviewer judgment. Rule preservation and rationale preservation are assessed separately; intentional status/history discard is recorded apart from accidental loss; deletion speed and mere existence of a replacement file are NOT evidence of a clean or lossless transfer (constitution §17, FR-037, SC-004, spec edge cases 10–11). If a previously absent FR-046 candidate is discovered post-BLIND, apply the Post-BLIND Candidate Discovery Rule before this phase can be considered complete. Gate: T031.
- [x] T033 [RS4] **BLOCKED UNTIL `challenge performed`** Absence-claim pass in `WORKING.md` §J using the authoritative FR-028 execution schema (constitution §13 minimum plus stricter execution fields): for every MATERIAL absence claim (materiality = FR-052) record (1) stable ID `A-001`…; (2) exact proposed claim; (3) intended analytical use / why material; (4) search terms; (5) synonyms/semantic variants; (6) documentary paths and historical path families searched; (7) Git refs searched; (8) relevant hits inspected; (9) residual uncertainty; (10) final safe wording. If corpus-wide absence cannot be established, weaken to search-bounded wording; never create absence records for trivial unused negatives (FR-028, SC-005, spec edge case 13). If a previously absent FR-046 candidate is discovered post-BLIND, apply the Post-BLIND Candidate Discovery Rule before this phase can be considered complete. Gate: T031.
- [x] T034 [RS4] **BLOCKED UNTIL `challenge performed`** `WORKING.md` §M Counter-Evidence is the authoritative pre-REPORT proposed-finding ledger. FIRST consolidate every material proposed final finding (materiality = FR-052; includes positive-finding candidates) into §M under stable finding IDs `F-001`, `F-002`, … — no records for trivial observations that are not candidate final findings. Each proposed-finding record contains at minimum: (1) finding ID; (2) concise proposed finding statement; (3) material supporting evidence-anchor IDs; (4) provenance-explicit material subclaims where needed; (5) provisional FR-049 severity; (6) provisional FR-050 confidence; (7) whether Critical eligibility is being proposed; (8) if Critical is proposed, the four FR-049 condition results individually; (9) strongest supporting evidence; (10) deliberate weakening / counter-evidence search result; (11) strongest plausible alternative interpretation; (12) post-counter-evidence severity; (13) post-counter-evidence confidence; (14) disposition: `retained` / `weakened` / `downgraded` / `removed`; (15) concise disposition rationale. THEN run the counter-evidence pass for EVERY proposed High/Critical finding: strongest supporting evidence; deliberate weakening search; strongest plausible alternative interpretation; severity reassessment; confidence reassessment; removal if counter-evidence wins — performed even when a finding seems obvious. Critical eligibility requires ALL FOUR FR-049 conditions; if any one fails the maximum severity is High. Confidence is rated independently per FR-050 and is never a substitute for provenance or severity. A provisional rating is NOT a final finding merely because it is written into §M; the final accepted severity/confidence after counter-evidence is the authoritative pre-REPORT rating; a finding marked `removed` must not silently appear in REPORT (constitution §18/§20, FR-029/049/050/052, SC-006, SC-014). If a previously absent FR-046 candidate is discovered post-BLIND, apply the Post-BLIND Candidate Discovery Rule before this phase can be considered complete. Gate: T032, T033.

### Late Proposed Finding Rule (authoritative — applies after the initial T034 pass and before final completion)

If any later permitted documentary analysis — especially the T035 current-state reread, T036–T038 REPORT synthesis, T041 wording resolution, or another pre-final evidence check — surfaces a NEW MATERIAL candidate final finding (FR-052) not yet represented in `WORKING.md` §M:

1. Do NOT invent it directly in REPORT, and do NOT discard it merely because T034 already ran.
2. Assign the next stable F-ID (`F-001`, `F-002`, …) and add a full §M proposed-finding record using the same T034 15-field schema.
3. Apply FR-052 materiality, then provisional FR-049 severity and FR-050 confidence; if Critical is proposed, evaluate all four FR-049 Critical conditions individually.
4. Perform the full T034 counter-evidence pass: strongest supporting evidence; deliberate weakening search; strongest plausible alternative interpretation; severity reassessment; confidence reassessment; `retained` / `weakened` / `downgraded` / `removed` disposition; concise disposition rationale.
5. If the finding is materially related to a user challenge hypothesis, perform a supplemental challenge evaluation against the already-supplied pack and record that delta in `WORKING.md` §L; do NOT rewrite BLIND.
6. Only a retained §M record may later become an accepted REPORT finding; removed findings remain excluded from accepted REPORT findings.
7. No new analytical artifact is created.

This is a conditional re-entry into the existing finding-evaluation discipline, not a new static task; task IDs remain unchanged and `WORKING.md` §M remains the authoritative proposed-finding / counter-evidence ledger.

---

## Phase 9 — Current-state reread — **BLOCKED UNTIL Phase 8 audits complete**

- [x] T035 [RS4] Reread the current documentary state into `WORKING.md` §N and classify each item as: current normative architecture / accepted limit / active plan / proposal / simplification proposal / historical explanation. Do not let current documents rewrite earlier historical intent, and treat later criticism of earlier architecture as `RD`/`RJ`-like documentary evidence rather than objective fact. Explicitly assess whether a current reader needs hidden historical knowledge to understand the present documentary architecture, keeping drift types (FR-033) and the seven complexity categories (FR-034) distinct (constitution §20/§21, FR-036, SC-012, Plan §17). If the current-state reread surfaces a new material proposed final finding absent from §M, apply the Late Proposed Finding Rule before T036 may begin — T035 must not silently mint REPORT findings. If a previously absent FR-046 candidate is discovered post-BLIND during this reread, apply the Post-BLIND Candidate Discovery Rule before Phase 9 can be considered complete. Gate: T034.

---

## Phase 10 — REPORT — **BLOCKED UNTIL `challenge performed`**

- [x] T036 [RS4] Create/populate `.review-bundles/docs-history-speckit/REPORT.md` using EXACTLY the FR-043 twenty-section order: 1 Executive Summary · 2 Current Documentary Architecture · 3 Historical Development · 4 Decision Threads · 5 Architecture Value Evolution · 6 Durable Ideas · 7 Reversals, Narrowings, and Abandoned Goals · 8 Source-of-Truth and Ownership History · 9 Scope Drift · 10 Focus Drift · 11 Value Drift · 12 Migration and Temporary-Machinery Audit · 13 Proof/Evaluation/Governance Expansion · 14 Artifact and Evidence Architecture · 15 Documentation Architecture · 16 Rationale Preservation and Retrospective Accuracy · 17 Findings · 18 What BeautyQ Got Right · 19 Documentation-Only Recommendations · 20 Coverage Appendix. Generate content FROM the `WORKING.md` ledgers and cited evidence anchors, NOT from remembered narrative. No draft or alternate report file; incremental population of this ONE file is permitted during Phase 10 only. REPORT findings come ONLY from retained §M records and REPORT is never its own source ledger: if REPORT synthesis itself reveals that a material finding is missing from §M, stop REPORT population for that finding, apply the Late Proposed Finding Rule, then resume only after its §M counter-evidence disposition exists. If a previously absent FR-046 candidate is discovered post-BLIND during synthesis, apply the Post-BLIND Candidate Discovery Rule before Phase 10 can be considered complete. Gate: T031 AND T032 AND T033 AND T034 AND T035 (FR-043, Plan §18, constitution §23).
- [x] T037 [RS4] Populate §17 Findings and §18 What BeautyQ Got Right. REPORT §17 findings MUST be populated from retained proposed-finding records in `WORKING.md` §M after the T034 counter-evidence pass; REPORT must not invent a finding, severity, or confidence that has no corresponding retained §M record, and every positive candidate finding that becomes a final REPORT finding likewise receives an F-ID and retained §M record before REPORT population. Each finding carries one FR-049 severity and an independent FR-050 confidence, cited material evidence anchors with per-anchor provenance, counter-evidence results, and (where used) the FR-027 retrospective classification; a composite finding is not assigned one forced provenance class — it combines individually provenance-explicit anchors and every material subclaim within it remains provenance-explicit (constitution §11 v1.0.2 / FR-026); High/Critical findings never rest only on `RD`/`CM`; positive findings use the same evidence standard as negative ones; fewer strong findings are preferred over volume; a material finding surfaced during population that lacks a retained §M record must first go through the Late Proposed Finding Rule (FR-026/029/035/049/050, constitution §11/§18/§22). Gate: T036.
- [x] T038 [RS4] Populate §9–§16 and §19/§20: keep the four drift types and seven complexity categories distinct with no assumed occurrence; keep proposals separate from current architecture; §19 recommendations are documentation-only; §20 Coverage Appendix shows candidate and excluded counts alongside the constitution-required COVERAGE counts, and epochs appear only as derived findings (FR-033–036, FR-043/045, SC-009, SC-012). Gate: T037.

---

## Phase 11 — Mechanical closeout and completion gate

- [x] T039 [P] [RS4] **BLOCKED UNTIL `challenge performed`** Recompute mechanically and read-only from source ledgers: candidate count; excluded-after-inspection count; relevant count; deep; semantic-scan; clerical; duplicate; earliest relevant date; latest relevant date; exact date span; historical documentary path count; finding IDs with severities and confidences — for finding recomputation the authoritative source ledger is the retained finding records in `WORKING.md` §M, compared mechanically against `REPORT.md` §17/§18 as applicable (REPORT is never its own finding source ledger); the retained §M records reconciled here include every finding added through the Late Proposed Finding Rule, and final F-IDs, severities, and confidences are taken mechanically from §M. Verify BOTH identities exactly (`candidate = relevant + excluded`; `relevant = deep + semantic-scan + clerical + duplicate`); verify that every final REPORT finding has exactly one retained F-ID, every retained F-ID intended for final reporting appears exactly once in REPORT, §M findings marked `removed` do not appear as accepted final findings, and REPORT severity/confidence values match the retained §M post-counter-evidence values exactly; no approximate forms such as `~30+` or `approximately` (FR-025, FR-045, constitution §8/§25, SC-003, SC-011). Parallel-safe with T040: both are non-mutating read-only final scans whose outputs cannot diverge. Gate: T038.
- [x] T040 [P] [RS4] **BLOCKED UNTIL `challenge performed`** Read-only absolute-wording occurrence scan of `REPORT.md` for: `always`, `never`, `only`, `entire`, `all`, `unbroken`, `unchanged`, `clean`, `sole`, `no document`, `completely`, `every` — produce the occurrence list with the supporting evidence status of each; make NO edits in this task (edits happen in T041) (constitution §25, FR-045). Parallel-safe with T039 as above. Gate: T038.
- [x] T041 [RS4] Resolve every T040 occurrence in `REPORT.md`: either cite documentary/absence-search support for the strong absolute or weaken the wording to search-bounded language; absence-derived wording must match its `A-xxx` record's final safe wording (FR-028, constitution §13/§25). Gate: T040.
- [x] T042 [RS4] DOC-only scope lint of `REPORT.md`: inspect for any claim about actual runtime behavior, actual source ownership, actual tests, actual CI, actual dependencies, actual Elasticsearch or Qdrant behavior, or implementation state, and rewrite each into documentary wording (e.g. "the technical specification still assigns … evidence ownership", not "X is the runtime owner"). NO code, test, build, or CI comparison may be performed to validate wording (constitution §26, FR-010, FR-045, SC-009). Gate: T041.
- [x] T043 [RS4] Cross-surface exact-value agreement over **final** counts: confirm every repeated count, date, and ID is identical across final `COVERAGE.tsv`, final `WORKING.md` §B/§C/§O, `REPORT.md` §20, and the intended completion response; do NOT compare final counts against BLIND's frozen §B freeze snapshot as though BLIND were a live final ledger (it is audited separately for internal correctness at freeze); if post-BLIND candidate additions occurred, verify `WORKING.md` contains the `freeze → final corpus delta` and that `BLIND.md` remains an untouched freeze snapshot except permitted clearly labelled `FACTUAL CORRECTION` appends; record the results in `WORKING.md` §O Final Mechanical Checks (FR-025/045, constitution §8/§25). Gate: T039, T042.
- [x] T044 [RS4] Canonical immutability gate: run BOTH `git diff -- README.md AGENTS.md docs` (constitution-required unstaged check) and `git diff --cached -- README.md AGENTS.md docs` (stricter non-conflicting execution check for staged canonical changes) — both MUST be empty. If either is non-empty, emit `Status: FAIL` per FR-053 (canonical diff state field reports both results), do NOT auto-revert, do NOT modify anything, do NOT claim completion (constitution §5, FR-005, SC-010). Gate: T043.
- [x] T045 [RS4] Issue `Status: COMPLETE` ONLY if every constitution/spec/plan/task gate above passed — including challenge state `challenge performed`, BLIND sealed, both identities exact, wording and scope lints clean, and BOTH the unstaged and staged canonical diffs empty — using the constitution §27 completion contract: exact paths of the four analytical outputs; frozen HEAD; historical range; candidate / excluded / relevant counts; deep / semantic-scan / clerical / duplicate counts; remaining coverage gaps; BLIND sealed status; challenge performed status; previous-review contamination status; the 3–5 strongest conclusions; and the exact statement `No implementation/code comparison was used as evidence.` COMPLETE is additionally forbidden while any post-BLIND candidate discovered under the Post-BLIND Candidate Discovery Rule remains unprocessed, while exact final §B/COVERAGE set reconciliation is incomplete, while a material post-BLIND delta has not been propagated through the affected audits, §M findings, and REPORT, or while a required `FACTUAL CORRECTION` append has not been made where a frozen BLIND material factual premise changed. Never COMPLETE before T031 (FR-040/053, constitution §27). Gate: T044.

---

## Dependencies & Execution Order

**Hard evidence sequence (Plan §21):**

`P1 (T001–T005) → P2 (T006–T007) → P3 (T008–T014) → P4 (T015–T017) → P5 (T018–T024) → P6/BLIND (T025–T027) → STOP (T028) → USER PACK (external) → P7 (T029–T031) → P8 (T032–T034) → P9 (T035) → P10 (T036–T038) → P11 (T039–T045)`

* **Phase-1 hard sequence (strictly sequential, no parallelism):** `T001 baseline → T002 Run-A isolation → T003 local-ref freeze → T004 documentary-path enumeration → T005 first analytical write`. No candidate discovery before T001 passes; **no historical documentary discovery occurs before T002 (Run-A isolation) passes** — Run-A isolation (T002) precedes any historical documentary candidate/path discovery (T004) and Phase 2 discovery (T006–T007); the T003 local-ref freeze bounds the review and precedes the T004 traversal; no analytical file before T005; no `COVERAGE.tsv` before T008.
* The exact unique-SHA Candidate Universe is persisted in `WORKING.md` §B at T006 before any disposition (T010) or reconciliation (T014); T007's non-primary-ref assessment completes the initial denominator (each previously absent unique commit added exactly once, shared commits never double-counted) BEFORE T009 derives runtime batches. T014 reconciles by exact SHA set membership against the current authoritative §B set (after all known late-candidate additions), not counts alone, and does NOT require thread IDs.
* T010 is one self-contained chronological runtime batch loop with classification, evidence-anchor creation, and per-batch WORKING compression inline; T011/T012/T013 are POST-LOOP audits over its completed output and never execute between its iterations.
* Late-candidate discovery (Phase-2 Late Candidate Discovery Rule) is a CONDITIONAL pre-BLIND execution correction, not a static dependency edge: when invoked it may require T010 processing of the new candidate, re-runs of the T011–T014 Phase-3 audits, and repetition of affected Phase-4/5 analysis before T025 passes. It introduces no new task IDs, no post-BLIND discovery policy, and no circular static task dependencies.
* `Phase 3 → evidence/accounting complete (T014)` → `Phase 5 → threads derived (T018)` → `Phase 6 pre-freeze → applicable COVERAGE thread IDs backfilled (T025)` before BLIND.
* Lineage (T015) and decisions (T016) precede thread synthesis (T018); threads precede cross-thread causality claims (T019); the Phase-5 chain `threads (T018) → cross-thread causality audit (T019) → retrospective audit (T020) / transfer seeding (T021) / blind open coding (T022) / candidate positive findings (T023)` completes before epoch derivation (T024) — T024 is the final interpretive Phase-5 synthesis step and cannot run immediately after T018 alone.
* Proposed final findings are consolidated with stable F-IDs and counter-evidence-tested in `WORKING.md` §M at T034 before REPORT population (T037); the retained §M records — not REPORT — are the authoritative finding source ledger that T039 reconciles against `REPORT.md` §17/§18.
* Post-BLIND candidate handling (Post-BLIND Candidate Discovery Rule, stated immediately before Phase 7) and post-T034 proposed-finding re-entry (Late Proposed Finding Rule, stated in Phase 8) are CONDITIONAL execution corrections introducing no new task IDs: both stay confined to the frozen local-ref universe and the existing four analytical outputs, and neither weakens T028 — no post-BLIND work runs until the external user-supplied challenge pack legitimately unlocks T029.
* **P7 (T029–T031) cannot start without the external user-supplied post-BLIND challenge pack**; T028 remains the mandatory hard external stop: a user-interaction boundary, not a task that flows into T029.
* **P10 (T036–T038) cannot start before P7 completes** (`challenge performed`); P8/P9 audits gate the report's findings.
* **T045 COMPLETE cannot occur before P11 passes** (T039–T044), and never while challenge state is `awaiting pack`.
* Any hard-gate failure anywhere: FR-053 `Status: FAIL`, stop downstream tasks. BLIND freeze: FR-053 `Status: PAUSED — awaiting challenge pack`.

**Parallelism note (deliberately conservative — historical interpretation is stateful):**

* `[P]` is used on exactly one non-diverging read-only pair: T039 ∥ T040 (final mechanical arithmetic recomputation ∥ final read-only wording occurrence scan, neither mutating shared interpretation). Phase 1 (T001–T005) is strictly sequential — T003 and T004 are NOT parallel-safe.
* Never parallelized: the T002 isolation gate (nothing documentary runs beside it), chronological candidate inspection or adjacent batches (T010–T013), lineage/decision reconstruction (T015–T017), thread and retrospective interpretation (T018–T023), epoch derivation (T024), BLIND synthesis (T025–T027), hypothesis challenge (T029–T031), transfer/absence/counter-evidence interpretation (T032–T034), current-state synthesis (T035), and REPORT drafting (T036–T038, T041–T042).

## Format Key

`- [ ] TNNN [P?] [RS#?] description` — `[P]` parallel-safe as constrained above; `[RS#]` = Research Story traceability label only (never execution order); no task is pre-checked; `T028` is the mandatory hard stop; `T029` is the first task blocked on the external challenge pack.
